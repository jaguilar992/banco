/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package banco;

/**
 *
 * @author jaguilar992
 */
public class Caja {
    // NO, Crea el codigo desde cero...
    //Desde Cero?
    // Antonio: Si, las funciones para cola aquí, no existen
    // Cliente posee una COLA, y se le agregan maximo 20 Clientes
    //A cliente no he llegado aún, eso le toca a Rafael
    // Trabaja en base a que un cliente ciertas propiedades (Transaccion, monto, necesidad de billetes)
    // Por eso teníamos que reunirnos :( Hay mucho que comentar
    //Esta bien, Gracias por la aclaración.
    
    // Empeza definiendo las propiedades, como billetera.
    // Luego como se creará
    
    
    
    // CAJA DEBE MANIPULAR UN OBJETO Transaccion -> Cliente (Propiedad de Cliente)
    // Puede ser de tipo: C, D o R (Heredado de Transaccion)(Ver Transacciones)
    // Segun el tipo debe determinarse el procedimiento, de acuerdo a lo que recibe)
    
    /**Controlador control; //Padre Controlador
    private int recesoRestante =0; //Turnos necesarios para abrir la caja
    protected int colaEsperaMax; //Suma los tiempos de espera de los clientes
    private int tamañoCola = 0;// Tamaño actual de la cola de clientes
    protected int maxTransaccionesPersona =0;// Maximo de transacciones que puede hacer un cliente;
    private int dinero =0;//Total dinero recaudado en la caja
    private int totalTransacciones =0;//Total de Transacciones que hizo el cliente
    private int totalCompradores =; //Total clientes que han pasado por la caja
    private boolean abierta=true; //Estado de la caja
    public Comprador primero = null; // Primer cliente que será atendido;
    
    public Caja(Controlador controlador); // Controlador es lo que llamaríamos Registro
        maxTransaccionesPersona = 3;
        colaEsperaMaxima = 5;
        control = controlador;
    }
    public void agregarCliente(Cliente cliente){
    
    if(primero == null){
        primero = cliente;
        tamañoCola = 1;
    } else{
    
    } cliente c;    
        for(c = primero; c.getSiguiente() !null; c = c.getSiguiente()){
        }
        c.setSiguiente(cliente);
        tamañoCola ++;
    }   
}
    public void atenderCliente()*/
    }